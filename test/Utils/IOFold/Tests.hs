module Utils.IOFold.Tests (test) where

import Test.Hspec
import Test.QuickCheck


test :: IO ()
test = hspec $ do
  describe "Modulo IOFold" $ do
      context "ioFoldl" $ do
        it "should" $ do
            pending

      context "ioFoldl" $ do
          it "should" $ do
            pending
